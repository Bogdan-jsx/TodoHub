import {StyleSheet} from 'react-native';
import styled from 'styled-components/native';

export const ChangeDateWrapper = styled.View`
  flex-direction: row;
  justify-content: space-between;
  align-items: center;
`;

export const styles = StyleSheet.create({
  addButton: {
    marginHorizontal: 8,
    marginVertical: 16,
  },
});
