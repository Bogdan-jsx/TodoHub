export enum SettingsActionType {
  SET_THEME = 'SET_THEME',
  SET_LANGUAGE = 'SET_LANGUAGE',
}

export enum Languages {
  English = 'en',
  Ukrainian = 'ua',
}

export enum ThemeType {
  Light = 'light',
  Dark = 'dark',
  System = 'system',
}
